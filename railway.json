{
  "services": [
    {
      "name": "BACKEND",
      "path": "RAILWAY_DEPLOYMENT/BACKEND",
      "buildCommand": "pip install -r requirements.txt",
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8000"
    }
  ]
}
